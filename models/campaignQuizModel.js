// load string asset for sweepstake based on camp id
const mongoose = require('mongoose');

const campaignQuizModel =  mongoose.Schema({

});

module.exports = mongoose.model('CampaignQuiz',campaignQuizModel);